/**
 * Feature Flags Configuration
 * Control feature toggles from a single central location.
 */
export const FEATURES = {
  AI_MENTOR: true,
  CAREER_MODE: true,
  CHALLENGE_ARENA: true,
  DAILY_CHALLENGE: true,
  ANALYTICS: true,
  KNOWLEDGE_GRAPH: true,
  DEVELOPER_MODE: true, // global enabling of dev capability (user toggles in settings)
};

export default FEATURES;
